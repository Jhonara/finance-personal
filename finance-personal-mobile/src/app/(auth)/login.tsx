import { Link } from 'expo-router';
import { Text, View } from 'react-native';

export default function LoginScreen() {
  return (
    <View>
      <Text>Login técnico pendiente de formulario.</Text>
      <Link href="/(auth)/register">Registro</Link>
    </View>
  );
}
